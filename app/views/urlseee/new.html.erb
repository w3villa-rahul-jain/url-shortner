<%= form_with model: @url, url: urls_path, method: :post do |form| %>
    <%= form.text_field :original_url, placeholder: 'Enter a long URL', class: 'form-control' %>
    <%= form.submit 'Shorten URL', class: 'btn btn-primary mt-3' %>
<% end %>   